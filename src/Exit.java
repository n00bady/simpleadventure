public class Exit {
    // This is the Exit class used to link Rooms together
    // TODO: Need to have:
    //                      all possible directions(String[]) both full and short names
    //                      where it leads to (Room)
    //                      It's probably better to assign numerical codes(Int) for each direction
    //                      and as always methods to set and get all the above
}
